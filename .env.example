# Supabase — copie este arquivo para .env.local e preencha com os valores reais.
# Encontre-os no painel do Supabase em: Project Settings > API
#
# .env.local NAO vai para o git (ver .gitignore). Este .env.example vai,
# apenas como modelo, sem segredos.

NEXT_PUBLIC_SUPABASE_URL=https://SEU-PROJETO.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=SUA-ANON-KEY
